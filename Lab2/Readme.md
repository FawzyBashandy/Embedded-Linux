Q1-Uploaded here

Q2-it worked , because i have copied the output file to the /usr/local/bin which is a directory that's already included in PATH Enviorment variable. and the shell can see all the files in that directory including my outputfile !

Q3-
1- cat /etc/shells

2-to see all the global variables -> run printenv

3-to display your current shell write -> echo ${SHELL}


Q4- '\' can be used in the terminal to indicate that the remaining of the command will be in the next line

Q5- alias PrintPath="echo ${PATH}"


